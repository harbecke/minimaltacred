conversion is to achieve huggingface compatible data format

baseline is a baseline classifier with some Matching The Blanks functionality

config is a sample config that has to be given to the baseline script as command line argument (not all options might have an effect)

data has two data reading helper functions
labels is a list of the tacred labels